Read `prd.json` and `progress.txt` to understand the current state.

Find the next story/step where `passes: false` and implement it.

Run any repo checks (tests, typecheck, lint) as needed.

Commit your changes with a clear message.

Update `prd.json` to mark completed work and append progress to `progress.txt`.

When ALL stories pass, output exactly: `<promise>COMPLETE</promise>`
