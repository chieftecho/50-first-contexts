# 50-first-contexts
50-first-contexts runs the Ralph autonomous loop using Kiro-CLI inside ephemeral Docker containers.  Each iteration starts from a completely fresh context — no memory, no hidden state — with progress persisted only through Git and the PRD.  Every run is like the first run.
